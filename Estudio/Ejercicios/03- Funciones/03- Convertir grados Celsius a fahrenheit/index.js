/* Dada una temperatura especificada en grados Celsius , mostrar el resultado de convertir el valor introducido a grados Fahrenheit. Fórmula de conversión

F = (C * 1.8) +32
*/

// function temperature(celsius) {
//     return fahrenheit = (celsius * 1.8) + 32;
// }

// temperature(60);

function celsiusToFahrenheit (celsius) {
    return (celsius * 1.8) + 32; 

}

