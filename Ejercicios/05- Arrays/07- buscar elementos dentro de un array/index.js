//escribir una funcion que reciba un elemento y un array como parámetro. La función debe comprobar que el elemento existe dentro del array buscando desde el principio

function find(element, list) {
    if (list.indexOf(element) !== -1) {
        return true; 
    } 
}; 

