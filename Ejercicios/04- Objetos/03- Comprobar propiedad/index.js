// escribir una función que compruebe si un objeto contiene la propiedad especificada

function hasProp(prop, obj) {
    if (prop !== undefined) {
        console.log('el objeto contiene esta propiedad');
    } else {
        console.log('esta propiedad no existe'); 
    }
    
}
